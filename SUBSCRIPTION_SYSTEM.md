# Система подписок - Руководство по настройке

## Обзор

Бот включает в себя комплексную систему подписок, которая позволяет пользователям приобретать премиум подписки различной продолжительности. Система интегрирована с платежным шлюзом YooKassa для безопасной обработки платежей.

## 🔑 Настройка API ключей

### Необходимые ключи для работы системы подписок

#### 1. YooKassa (Яндекс.Касса) - Основной платежный провайдер

**Получение ключей:**
1. Зарегистрируйтесь в [YooKassa](https://yookassa.ru/)
2. Пройдите процедуру верификации
3. В личном кабинете перейдите в раздел "Настройки" → "API ключи"
4. Скопируйте:
   - **Shop ID** (Идентификатор магазина)
   - **Secret Key** (Секретный ключ)

**Где вставлять ключи:**
```python
# В файле payment_service.py замените тестовые значения на реальные:

# ТЕСТОВЫЕ (для разработки):
TEST_SHOP_ID = "1000000"
TEST_SECRET_KEY = "test_secretkey"

# РЕАЛЬНЫЕ (для продакшена):
SHOP_ID = "ваш_shop_id_здесь"
SECRET_KEY = "ваш_secret_key_здесь"
```

#### 2. Telegram Bot API

**Уже настроено в `.env`:**
```bash
BOT_TOKEN=your_telegram_bot_token_here
BOT_USERNAME=your_bot_username
```

### 🛠 Пошаговая настройка

#### Шаг 1: Настройка YooKassa

1. **Создайте аккаунт YooKassa:**
   - Перейдите на https://yookassa.ru/
   - Нажмите "Подключиться"
   - Заполните данные компании/ИП

2. **Получите API ключи:**
   ```
   Личный кабинет → Настройки → API и вебхуки → Ключи API
   ```

3. **Настройте вебхуки (опционально):**
   ```
   URL: https://ваш_домен.com/webhook/yookassa
   События: payment.succeeded, payment.canceled
   ```

#### Шаг 2: Обновление кода

Откройте файл `payment_service.py` и обновите настройки:

```python
class YooKassaService:
    # Замените на ваши реальные данные
    SHOP_ID = "ваш_shop_id"
    SECRET_KEY = "ваш_secret_key"
    
    # Для тестирования оставьте:
    # TEST_SHOP_ID = "1000000"
    # TEST_SECRET_KEY = "test_secretkey"
```

#### Шаг 3: Настройка цен

Обновите цены подписок в том же файле:

```python
SUBSCRIPTION_PRICES = {
    "1month": 299.00,    # 1 месяц - 299₽
    "3months": 799.00,   # 3 месяца - 799₽
    "6months": 1499.00,  # 6 месяцев - 1499₽
    "1year": 2799.00     # 1 год - 2799₽
}
```

## 💡 Функции системы

### Возможности для пользователей
- **Кнопка подписки**: Доступна в меню профиля пользователя
- **4 тарифа подписки**: 1 месяц, 3 месяца, 6 месяцев и 1 год
- **Автоматическое продление**: При наличии активной подписки новые покупки продлевают текущий период
- **Отслеживание статуса**: Пользователи могут просматривать текущий статус подписки, дату истечения и оставшиеся дни
- **Безопасные платежи**: Интеграция с тестовым шлюзом YooKassa

### Технические возможности
- **Интеграция с БД**: Отдельная таблица `subscriptions` для хранения данных подписок
- **Отслеживание платежей**: Каждая подписка связана с ID платежа для аудита
- **Управление истечением**: Автоматическое определение просроченных подписок
- **Логика продления**: Интеллектуальная обработка обновлений и продлений подписок

## 📊 Тарифы подписок

| Период | Цена | Дни | Описание |
|---------|------|-----|----------|
| 1 месяц | 299₽ | 30 | Месячная подписка |
| 3 месяца | 799₽ | 90 | Квартальная подписка (Лучшее предложение) |
| 6 месяцев | 1499₽ | 180 | Полугодовая подписка |
| 1 год | 2799₽ | 365 | Годовая подписка (Максимальная экономия) |

## 🔄 Пользовательский процесс

1. **Доступ к подписке**: Пользователь нажимает кнопку "💎 Подписка" в меню профиля
2. **Просмотр вариантов**: Бот отображает тарифы подписок с ценами
3. **Выбор периода**: Пользователь выбирает желаемую продолжительность подписки
4. **Оплата**: Пользователь перенаправляется на страницу оплаты YooKassa
5. **Активация**: После успешной оплаты подписка автоматически активируется
6. **Управление**: Пользователь может продлить существующие подписки или просмотреть статус

## 🗄️ Схема базы данных

### Таблица подписок
```sql
CREATE TABLE subscriptions (
    telegram_id INTEGER PRIMARY KEY,
    subscription_type TEXT NOT NULL,
    start_date TEXT NOT NULL,
    end_date TEXT NOT NULL,
    is_active INTEGER NOT NULL DEFAULT 1,
    payment_id TEXT,
    amount REAL,
    currency TEXT DEFAULT 'RUB',
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
)
```

## 🔌 Интеграция с API

### Конфигурация YooKassa

**Тестовые данные (для разработки):**
```python
TEST_SHOP_ID = "1000000"
TEST_SECRET_KEY = "test_secretkey"
```

**Продакшн данные (замените на свои):**
```python
SHOP_ID = "ваш_реальный_shop_id"
SECRET_KEY = "ваш_реальный_secret_key"
```

**API конфигурация:**
- **API Endpoint**: `https://api.yookassa.ru/v3`
- **Валюта**: RUB (Российские рубли)
- **Метод авторизации**: Basic Auth (Shop ID + Secret Key)

### 🔄 Процесс оплаты
1. Создание запроса на оплату с метаданными подписки
2. Перенаправление пользователя на страницу оплаты YooKassa
3. Мониторинг статуса оплаты через опрос API
4. Обработка успешных платежей и активация подписок
5. Корректная обработка неудачных платежей

## 💻 Компоненты кода

### Модели
- `models/subscription.py`: Модель данных подписки с логикой истечения
- Операции с БД в `database.py` для управления подписками

### Сервисы
- `payment_service.py`: Интеграция с YooKassa и обработка платежей
- Конфигурация цен и создание/проверка платежей

### Пользовательский интерфейс
- `keyboards.py`: Клавиатуры и кнопки для подписок
- `handlers.py`: Обработчики сообщений и callback для процесса подписки
- `message_generator.py`: Генерация меню подписок и отображение статуса

### Ключевые методы
- `handle_subscription_menu()`: Отображение вариантов подписки
- `handle_subscription_period_selection()`: Обработка выбора периода и создание платежа
- `extend_subscription()`: Продление существующей подписки
- `get_subscription()`: Получение текущего статуса подписки пользователя

## ⚙️ Конфигурация

### Переменные окружения

Система подписок использует существующую конфигурацию бота. Дополнительные переменные окружения не требуются для базовой функциональности.

**Обязательные переменные (уже настроены в `.env`):**
```bash
BOT_TOKEN=ваш_telegram_bot_token
BOT_USERNAME=имя_вашего_бота
COC_API_TOKEN=ваш_coc_api_token
```

### Настройки для продакшена

Для развертывания в продакшене рекомендуется:

1. **Обновить учетные данные YooKassa на реальные:**
   ```python
   # В payment_service.py
   SHOP_ID = "ваш_реальный_shop_id"
   SECRET_KEY = "ваш_реальный_secret_key"
   ```

2. **Настроить вебхуки для уведомлений о платежах в реальном времени**
3. **Добавить преимущества подписки и ограничения функций**
4. **Настроить автоматические уведомления об истечении**

## 🧪 Тестирование

Для проверки совместимости и функциональности используется:
- Тест совместимости системы: `test_compatibility.py`
- Валидация компонентов и функций
- Проверка интеграции с базой данных

Запуск тестов:
```bash
python test_compatibility.py
```

**Примечание**: Полные модульные тесты для системы подписок могут быть добавлены по мере необходимости.

## 🔒 Вопросы безопасности

- Данные платежей обрабатываются безопасно через YooKassa
- Локально не хранится конфиденциальная платежная информация
- ID платежей используются для отслеживания транзакций и аудита
- Аутентификация пользователей через Telegram ID

## 🚀 Будущие улучшения

Потенциальные улучшения системы подписок:
- Интеграция вебхуков для уведомлений о платежах в реальном времени
- Преимущества подписки и премиум функции
- Автоматические напоминания о продлении
- Аналитика использования и отчеты
- Промокоды и специальные цены
- Семейные/групповые тарифы подписок

## 🛠️ Устранение неполадок

### Частые проблемы

1. **Ошибка "Invalid credentials"**
   - Проверьте правильность Shop ID и Secret Key
   - Убедитесь, что используете правильные ключи (тестовые/продакшн)

2. **Платежи не обрабатываются**
   - Проверьте статус аккаунта YooKassa
   - Убедитесь, что API ключи активны
   - Проверьте логи в файле `bot.log`

3. **Подписки не активируются**
   - Проверьте работу базы данных
   - Убедитесь, что таблица `subscriptions` создана
   - Проверьте права доступа к БД

### Логи и отладка

Все операции логируются в файл `bot.log`. Для отладки увеличьте уровень логирования:

```python
# В main.py
logging.basicConfig(level=logging.DEBUG)
```

### Контакты поддержки

При возникновении проблем:
1. Проверьте логи в `bot.log`
2. Убедитесь в правильности настройки API ключей
3. Проверьте доступность API YooKassa
4. Обратитесь к документации YooKassa